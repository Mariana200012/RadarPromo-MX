name: Automatización RadarPromo-MX

on:
  schedule:
    - cron: '*/45 * * * *' # Se ejecuta cada 2 horas automáticamente
  workflow_dispatch: # Botón para ejecutar manualmente

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install telethon python-dotenv requests

      - name: Ejecutar Bot
        env:
          # Estas son las "llaves" que configuraste en Secrets
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        run: python bot.py

      - name: Guardar Historial (DB)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add radar_promo.db
          git commit -m "Actualizar DB de ofertas [skip ci]" || echo "Sin cambios"
          git push
